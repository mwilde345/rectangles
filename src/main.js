class Main {
    // first check they are rectangles
    checkAdjacent(r1, r2) {
        // isParallel helper for each pair of lines between rectangles. stop once you find the first
        // parallel pair. then you know all related sides between the two are parallel. if none parallel, no adjacency
        
    }

    checkContains(r1, r2) {

    }

    checkIntersection(r1, r2) {

    }

    
}